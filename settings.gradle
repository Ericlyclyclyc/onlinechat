pluginManagement {
    repositories {
        gradlePluginPortal()
        // NeoGradle 6 (userdev) is published on the NeoForged maven, not the plugin portal.
        maven { url = 'https://maven.neoforged.net/releases' }
    }
}
